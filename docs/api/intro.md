---
title: API Reference
---

This section documents the Ciplaw gateway API. Focused, example-driven, and precise. For machine-readable schema see the OpenAPI spec at `/openapi/ciplaw-gateway.v1.yaml`.

Primary endpoints:
- `POST /v1/verify` — submit an envelope for verification.
- `GET /v1/verify/{id}` — retrieve verification result.
